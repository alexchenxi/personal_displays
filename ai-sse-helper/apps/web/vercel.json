{
  "framework": "nextjs",
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && npx turbo build --filter=web",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": ".next"
}